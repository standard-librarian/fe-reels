# server {
#     listen       80;
#     server_name  _;
#     root   /usr/share/nginx/html;
#     index  index.html;

#     # Compress text responses on the fly.
#     gzip            on;
#     gzip_vary       on;
#     gzip_min_length 1024;
#     gzip_types      text/plain text/css application/javascript application/json
#                     application/xml image/svg+xml font/woff2;

#     # Vite fingerprints asset filenames (content hash), so they can be cached
#     # forever — a new deploy ships new filenames.
#     location /assets/ {
#         expires    1y;
#         add_header Cache-Control "public, immutable";
#         try_files  $uri =404;
#     }

#     # SPA fallback: unknown paths serve index.html so client-side entry (and any
#     # deep link) works. index.html itself is never cached, so a deploy is picked
#     # up immediately.
#     location / {
#         add_header Cache-Control "no-cache";
#         try_files  $uri /index.html;
#     }
# }
