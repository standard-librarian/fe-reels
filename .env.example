# Base URL for the Reels API. Leave empty to use the same origin, which the
# MSW mock backend intercepts in dev. Point this at the real backend to go live.
VITE_API_BASE_URL=

# Set to false to hit the network directly instead of the MSW mock (dev only).
VITE_API_MOCK=true
